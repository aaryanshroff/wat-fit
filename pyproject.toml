[project]
name = "wat-fit"
version = "0.1.0"
description = "UWaterloo facility occupancy checker"
authors = [
    {name = "Aaryan Shroff",email = "aaryan.shroff@uwaterloo.ca"}
]
requires-python = ">=3.11"
dependencies = [
    "requests (>=2.32.5,<3.0.0)",
    "beautifulsoup4 (>=4.14.3,<5.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
